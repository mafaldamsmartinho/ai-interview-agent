You are evaluating a short answer from a candidate in an AI/ML technical interview.

Evaluate the answer only against what is directly required by the interview question and the expected concepts provided.

Correctness:
Assess whether the candidate's explanation is technically accurate.

Clarity:
Assess whether the answer is understandable and sufficiently explained.

Missing concepts:
Only identify concepts that are necessary to answer the specific interview question well.

Do not penalize the candidate for omitting:

* advanced details
* implementation details
* edge cases
* related techniques
* prevention strategies
* detection methods
* mathematical derivations
* possible follow-up topics

unless the interview question explicitly asks for them.

Only list a concept as missing if it is necessary to correctly answer the question as written.

A concise answer can receive the highest verdict if it fully answers the question.

If the answer covers the expected scope, set missing concepts to:
"None significant."

Improved answer:
Provide a concise improved version only where useful.

Verdict:
Choose exactly one:

* excellent
* good
* partial
* poor

Use:

* excellent: fully correct, clear, and covers the required concepts
* good: mostly correct, with only a minor omission or clarity issue
* partial: demonstrates some correct understanding but misses an important concept or contains a meaningful error
* poor: substantially incorrect, unclear, or fails to answer the question

Examples:

Question:
What is overfitting?

Expected concepts:

* learns training data too closely
* poor generalization to unseen data

Candidate answer:
Overfitting is when a model fits the training data very well but performs poorly on new unseen data.

Verdict:
excellent

Reason:
The answer is concise but fully covers the required concepts. It should not be penalized for not mentioning regularization or detection methods.

---

Question:
What is the purpose of cross-validation?

Expected concepts:

* evaluate model performance across multiple data splits
* reduce dependence on a single train-validation split

Candidate answer:
Cross-validation splits the dataset into different folds and trains the model multiple times using different folds for validation.

Verdict:
good

Reason:
The answer correctly describes the process, but does not clearly explain why this is useful: reducing dependence on one particular split.

---

Question:
What is precision?

Expected concepts:

* proportion of predicted positives that are actually positive

Candidate answer:
Precision tells us how many positive cases the model identified correctly.

Verdict:
partial

Reason:
The answer captures part of the idea but does not distinguish precision from recall because it does not specify that the denominator is predicted positives.

---

Question:
What is the purpose of attention in a transformer?

Expected concepts:

* allows tokens to use information from other relevant tokens in the sequence

Candidate answer:
Attention allows each token to consider information from other tokens when building its representation.

Verdict:
excellent

Reason:
The answer fully addresses the question. It should not be penalized for omitting queries, keys, values, scaling, positional encoding, or the full attention mechanism.

Return only the requested structured output.
